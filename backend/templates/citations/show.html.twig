{% extends 'base.html.twig' %}

{% block title %}Citations{% endblock %}

{% block body %}
    <h1>Citations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ citation.id }}</td>
            </tr>
            <tr>
                <th>Resume</th>
                <td>{{ citation.resume }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('citations_index') }}">back to list</a>

    <a href="{{ path('citations_edit', {'id': citation.id}) }}">edit</a>

    {{ include('citations/_delete_form.html.twig') }}
{% endblock %}
